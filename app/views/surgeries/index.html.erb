<h4> <%= Time.now.strftime('%b %-d, %Y %r') %> 
  <%= link_to 'New', new_surgery_path, class: 'btn btn-small btn-primary pull-right' %></h4>

<table class="table table-hover table-condensed">
<% @surgeries.each do |surgery| %>
  <tr>
    <td>
      <p class="lead" style="color: <%= surgery.case_color %>"> 
      <%= surgery.surgery %></p>
      <p><%= surgery.surgeon.name %> (<%= surgery.or_room %> - <%= surgery.time_estimate %>min)<%= link_to 'Edit', edit_surgery_path(surgery) %> </p> 
      <p>Estimated start: 5:30pm</p></td></tr> 
<% end %>
</table>

<br />


 
<% if false %>
   ...

  <!-- <td><%= surgery.anesthesia_id %></td>
    <td><%= surgery.case_cart %></td>
    <td><%= surgery.finished %></td>
    <td><%= surgery.finished_at %></td>
    <td><%= surgery.notes %></td>
    <td><%= surgery.operation_date %></td>
    <td><%= surgery.or_room %></td>
  </tr>
  <tr>
    <td><%= surgery.patient_id %></td>
    <td><%= surgery.position %></td>
    <td><%= surgery.started %></td>
    <td><%= surgery.started_at %></td>

  
    <td><%= surgery.time_estimate %></td>
    <td><%= surgery.triage %></td><td><%= link_to 'Show', surgery %></td>
    <td><%= link_to 'Edit', edit_surgery_path(surgery) %></td>
    <td><%= link_to 'Destroy', surgery, method: :delete, data: { confirm: 'Are you sure?' } %></td>
  </tr>
    --><% end %>